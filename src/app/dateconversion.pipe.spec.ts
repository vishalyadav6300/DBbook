import { DateconversionPipe } from './dateconversion.pipe';

describe('DateconversionPipe', () => {
  it('create an instance', () => {
    const pipe = new DateconversionPipe();
    expect(pipe).toBeTruthy();
  });
});
