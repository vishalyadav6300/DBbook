import { LiveStatusDirective } from './live-status.directive';

describe('LiveStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveStatusDirective();
    expect(directive).toBeTruthy();
  });
});
