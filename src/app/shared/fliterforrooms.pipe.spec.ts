import { FliterforroomsPipe } from './fliterforrooms.pipe';

describe('FliterforroomsPipe', () => {
  it('create an instance', () => {
    const pipe = new FliterforroomsPipe();
    expect(pipe).toBeTruthy();
  });
});
